# Copy this file to secrets.yaml and replace every placeholder.
# Never commit secrets.yaml.

api_encryption_key: "REPLACE_WITH_YOUR_32_BYTE_BASE64_KEY"
wifi_setup_password: "REPLACE_WITH_A_STRONG_SETUP_AP_PASSWORD"

# Optional only if you also add `password: !secret ota_password`
# to the OTA block in solarflow_ble_controller.yaml:
# ota_password: "REPLACE_WITH_A_STRONG_OTA_PASSWORD"
